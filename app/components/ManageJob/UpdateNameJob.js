import { View, Text } from "react-native";
import React from "react";

export default function UpdateNameJob() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Update name Job</Text>
    </View>
  );
}
